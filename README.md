## prerequisites
1. Python 3.12
2. uv

## setup
1. Copy `.env.example` to `.env` and fill missing values for vars.
2. Try different models.
   - for `openai.gpt-4o`, application works correctly
   - for `gemini-2.5-pro`, tool calls do not work
